# Dependencies
node_modules/

# Wrangler (local cache per worker)
*/.wrangler/

# Environment secrets
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# MiniMax Code local cache
.mimocode/

# (legacy comment 2026-06-10: helper scripts 统一放 .tmp/ 子目录, 绝不入库)

# Test / dev workers
csnews-h1-test/

# Supabase CLI temp files
supabase/.temp/

# Local-only directories · never push (planning docs, hooks, scripts)
tasks/
specs/
.githooks/
scripts/
verifier/

# Privacy tooling (local, by-design contain forbidden terms)
# 本地开发者 npm install 后 prepare hook 自动 cp .privacy-patterns.txt.example → .privacy-patterns.txt
.privacy-patterns.txt
.privacy-patterns.txt.example
csnews-agent/scripts/check-privacy.sh
csnews-agent/validate/privacy-grep.contract.ts
# 排除测试文件（validate/ 目录整体不入仓库）
csnews-agent/validate/
csnews-fission/validate/
csnews-mcp-server/validate/

# 诊断产物 (curl 探测 / R2 抓取)
csnews-agent/diagnostic/
csnews-agent/news/

# Wrangler local cache + build outputs
.wrangler/
csnews-agent/.wrangler/
csnews-agent/dist/
csnews-agent/coverage/

# Spec Kit local-only — never push (contain internal codenames + KR refs)
.agents/
.specify/
AGENTS.md
csnews-agent/AGENTS.md
!csnews-agent/package.json
!csnews-agent/package-lock.json
csnews-agent/src/test-helpers.ts
.mavis/

# Supabase CLI generated config (from supabase link)
supabase/supabase/

# Local fission R2 write test artifact
fission/
