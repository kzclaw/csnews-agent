#!/bin/bash
# ============================================================
# .githooks/pre-commit
# kzclaw 2026-06-12 21:40 确定 v0.36.3
# pre-commit hook: 调用 scripts/bump-version.sh
# ============================================================

REPO_ROOT="$(git rev-parse --show-toplevel)"

if [ -f "$REPO_ROOT/scripts/bump-version.sh" ]; then
  bash "$REPO_ROOT/scripts/bump-version.sh" || exit 1
fi
