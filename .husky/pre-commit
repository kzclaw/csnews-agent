#!/usr/bin/env bash
# Pre-commit hook · delegates to scripts/check-privacy.sh
# Auto-configured by package.json "prepare" script: core.hooksPath = csnews-agent/.githooks
# Mavis 23:37 拍板 C: check-privacy.sh gitignored (local tooling, optional for new contributors)

set -e
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
SCRIPT_PATH="$SCRIPT_DIR/scripts/check-privacy.sh"

if [ ! -f "$SCRIPT_PATH" ]; then
  echo "⚠️ check-privacy.sh not found (gitignored, optional)"
  echo "   Privacy check skipped. Restore from git history to enable:"
  echo "     git checkout HEAD~1 -- csnews-agent/scripts/check-privacy.sh"
  exit 0
fi

bash "$SCRIPT_PATH"