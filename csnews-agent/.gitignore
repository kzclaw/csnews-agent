# Dependencies
node_modules/

# Local tools (viewers that need personal config to run)
# tools/*.html
# !tools/.gitkeep
# 注: pull-viewer.html 已经被验证为通用占位符模板, 不烤任何真实 URL/token, 可以入库
# 如未来加新工具含本地信息, 重新启用上面两行忽略规则

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Wrangler
.wrangler/
.dev.vars

# Local helper scripts (含 SUPABASE_SERVICE_KEY / BEARER_TOKEN 等敏感信息, 不入库)
# 2026-06-10 确定: 任何 helper 脚本统一放 .tmp/ 子目录, 绝不入库
.tmp/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.local
.env.*.local

# Local privacy patterns (per-developer, copy from .privacy-patterns.txt.example at repo root)
.privacy-patterns.txt
# wrangler prod config (real IDs)
wrangler.prod.toml
