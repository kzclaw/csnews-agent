{
  "name": "csnews-agent-root",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "ocr": "ocr review --rule .ocr-rules.json --audience agent --format text",
    "ocr:prepush": "ocr review --rule .ocr-rules.json --audience agent --format text"
  }
}
